pipeline {
  agent any
  stages {
  stage('step1') {
if (currentBuild.result == 'success') {
 sh 'echo Build successfully executed and trigger to testing server'
}
else (currentBuild.result == 'failure'){
  sh "echo 'Build failure'"
} 
  
}
}
  }  
