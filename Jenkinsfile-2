pipeline {
  agent any
  stages {
    stage('Hello') {
      steps {
        sh 'echo Hello Chennai'
      }
      post {
        success {
          script {
            currentBuild.result = 'UNSTABLE'
  }
  post {
    always {
      echo currentBuild.currentResult
    }
  }
}
