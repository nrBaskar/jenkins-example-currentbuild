pipeline {
  agent any
  stages {
    stage('Hello') {
      steps {
        sh 'echo Hello Chennai'
      }
      post {
        success {
          script {
            currentBuild.result = 'FAILED'
  }
  post {
    always {
      echo currentBuild.currentResult
    }
  }
}
